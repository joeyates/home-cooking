#!/bin/sh

# ensure the requested files exist
for f; do
  test -e "$f" || touch "$f"
done

if [ $# -gt 0 ]; then
  # ensure a new tab is created
  mvim --servername VIM --remote-tab-silent  "$@"
else
  mvim --servername VIM
fi

